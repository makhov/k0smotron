apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Apply with: kubectl apply -k bench/infra/manifests/monitoring
# (After kube-prometheus-stack is up — provides ServiceMonitor CRD.)

resources:
  - k0smotron-metrics-service.yaml
  - k0smotron-servicemonitor.yaml
  - k0s-pushgateway-servicemonitor.yaml
